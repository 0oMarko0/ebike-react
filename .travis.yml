language: node_js
node_js: node
cache:
  directories:
    - node_modules
install:
  - yarn install
script:
  - yarn install
before_deploy:
  - yarn build
deploy:
  provider: script
  script: yarn deploy
  on:
    branch: $DEPLOYMENT_BRANCH

